const leaderboard = {
    saveScore: function() {
        // playerName = flappyCookies.parse()['player_name'];
        // console.log(playerName);
        // if (playerName && playername != "") {

        // }
    }
}